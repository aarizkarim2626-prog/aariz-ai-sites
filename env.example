# Copy this file's variable into Vercel's dashboard instead of committing a real .env
# Settings → Environment Variables → add GROQ_API_KEY with your real key as the value
GROQ_API_KEY=your_groq_api_key_here
